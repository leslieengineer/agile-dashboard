import {randomBytes,scrypt,timingSafeEqual,type ScryptOptions} from 'node:crypto'
const PARAMS={N:16384,r:8,p:1};function derive(password:string,salt:Buffer,length:number,options:ScryptOptions){return new Promise<Buffer>((resolve,reject)=>scrypt(password,salt,length,options,(e,key)=>e?reject(e):resolve(key as Buffer)))}
export async function hashPassword(password:string){const salt=randomBytes(32),key=await derive(password,salt,32,{...PARAMS,maxmem:64*1024*1024});return`scrypt$N=${PARAMS.N},r=${PARAMS.r},p=${PARAMS.p}$${salt.toString('base64')}$${key.toString('base64')}`}
export async function verifyPassword(password:string,record:string){try{const[,params,salt64,key64]=record.split('$');if(!params||!salt64||!key64)return false;const v=Object.fromEntries(params.split(',').map(x=>x.split('='))),expected=Buffer.from(key64,'base64'),actual=await derive(password,Buffer.from(salt64,'base64'),expected.length,{N:Number(v.N),r:Number(v.r),p:Number(v.p),maxmem:64*1024*1024});return expected.length===actual.length&&timingSafeEqual(expected,actual)}catch{return false}}
